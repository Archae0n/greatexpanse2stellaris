namespace = no_starbase_defense

event = {
	id = no_starbase_defense.1	
	hide_window = yes
	is_triggered_only = yes
	#fire_only_once = yes
	
	immediate = {
		every_country = {
			limit = {
				is_ai = yes
				is_country_type = "default"
				used_naval_capacity_percent < 0.90
				has_monthly_income = {
					resource = energy
					value > 75
				}
				has_monthly_income = {
					resource = minerals
					value > 75
				}
			}
			add_modifier = {
				modifier = no_platforms
				days = 360
			}
		}
	}
}


