
cb_demilitarize = { #PeaceKeeping Intervientionists
	show_in_diplomacy = yes
	show_notification = yes
	potential = {
		has_valid_civic = civic_peacekeeping_interventionists		
	}
	is_valid = {
		OR = {
			is_subject = no
			overlord = { NOT = { is_same_value = from } }
		}
	}
}

cb_neurotoxins = { #NeuroToxins
	show_in_diplomacy = yes
	show_notification = yes
	potential = {
		has_valid_civic = civic_neurotoxins		
	}
	is_valid = {
		OR = {
			is_subject = no
			overlord = { NOT = { is_same_value = from } }
		}
	}
}