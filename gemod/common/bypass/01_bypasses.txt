alpha_gate = {
	name = ALPHA_GATE_GALACTIC
	is_pathfind = yes
	uses_action = no
	always_active = no
	windup_time = 0
	winddown_time = 0
	ftl_multiplier = 0			# 0 means instant
	icon_frame = 25				# was 29 25 = gateway / 12 = wormhole / 29 = lgate?
	remember_instances = no 	# whether countries remember meeting a specific instance of that type, or if they just remember seeing the type in general
	galactic_map_tooltip_header = ALPHA_GATE_GALACTIC
	galactic_map_tooltip_desc = ALPHA_GATE_GALACTIC_DESC
	connection_type = scripted_connection
	animation_time = 1
	ftl_ship_effect = "gateway_ftl_ship_effect_entity"
	ftl_bypass_effect = "gateway_ftl_bypass_effect_entity"

	country_can_use = {
		custom_tooltip = {
			fail_text = ALPHA_GATE_COUNTRY_CANNOT_USE
			has_global_flag = alpha_gates_activated_globally
		}
	}
	
	scripted_connection = {
	
		# defines endpoints that can only reach the "nexus"
		node = {
			# system scope
			has_star_flag = alpha_gate
		}
	
		# defines endpoint(s) that can reach all "nodes" (but not other nexuses)
		nexus = {
			# system scope
			has_star_flag = alpha_cluster_nexus
		}
	}
}