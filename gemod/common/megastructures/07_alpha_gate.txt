# see README.txt for example

alpha_gate_0 = {
	entity = "construction_platform_entity"
	construction_entity = "construction_platform_entity"
	portrait = "GFX_megastructure_construction_background"
	place_entity_on_planet_plane = no
	#build_outside_gravity_well = yes 
						
	show_galactic_map_icon = no

	build_time = 720
	build_cost = {
		minerals = 5000
		influence = 50
	}
	construction_blocks_others = no

	potential = {
		has_technology = tech_alpha_gate
	}

	possible = {
		custom_tooltip = {
			fail_text = "requires_inside_border"
			is_inside_border = from
		}
		custom_tooltip = {
			fail_text = "requires_surveyed_system"
			NOT = {
				any_planet = {
					is_surveyed = {
						who = prev.from
						status = no
					}
				}
			}
		}
		custom_tooltip = {
			fail_text = "requires_no_existing_gateway"
			NOR = {
				has_megastructure = alpha_gate
			}
		}
		custom_tooltip = {
			fail_text = "requires_technology_alpha_gate_construction"
			from = { has_technology = tech_alpha_gate }
		}
	}

	placement_rules = {			# those would be ignored since the gateway is being "free-placed" between the inner and outer radius of the system
	}
	
	# root = system
	# from = country
	ai_weight = {
		factor = 0		
	}
	
	on_build_start = {}
	on_build_cancel = {}	
	on_build_complete = {
		from = { country_event = { id = alpha_gate.88 } }
	}
}

alpha_gate = {
	entity = "alpha_gateway_entity"
	construction_entity = "gateway_portal_entity"
	portrait = "GFX_megastructure_construction_background"
	place_entity_on_planet_plane = no
	# build_outside_gravity_well = yes 						

	show_galactic_map_icon = no
	potential = {
		has_technology = tech_alpha_gate
	}
	possible = {
		custom_tooltip = {
			fail_text = "requires_technology_alpha_gate_construction"
			from = { has_technology = tech_alpha_gate }
		}
	}
	upgrade_from = {
		alpha_gate_0
	}
	build_time = 1800
	build_cost = {
		minerals = 10000
		energy = 5000
	}
	construction_blocks_others = no
	bypass_type = alpha_gate
	on_build_complete = {
		set_star_flag = alpha_gate
		activate_gateway = fromfrom
		#owner = { 
		#	country_event = { id = alpha_gate.87 }
		#}
	}
}